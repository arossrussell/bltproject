local:
    root: /var/www/new-project/docroot
    uri: 'http://local.new-project.com'
    host: local.new-project.com
    user: vagrant
    ssh: { options: '-o PasswordAuthentication=no -i $HOME/.vagrant.d/insecure_private_key' }
